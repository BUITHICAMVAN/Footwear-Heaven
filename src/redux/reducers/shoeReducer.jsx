import { createSlice } from '@reduxjs/toolkit'

const initialState = {
}

const shoeReducer = createSlice({
    name: 'shoeReducer',
    initialState,
    reducers: {
    }
});

export const {} = shoeReducer.actions

export default shoeReducer.reducer